# Database (Legacy Path)

This folder is retained for backward compatibility only.

The ETL loader and related assets were moved to `apps/db`.

Use:
- `apps/db/README.md`
- `python apps/db/scripts/etl_loader.py`

Legacy command compatibility:
- `python apps/database/script/etl_loader.py` still works and forwards to the new loader.